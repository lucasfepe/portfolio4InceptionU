schemaVersion: 2.0.0
components:
  - name: dev
    container:
      image: public.ecr.aws/aws-mde/universal-image:latest
commands:
  - id: install
    exec:
      component: dev
      commandLine: "npm install http-server -g"
  - id: build
    exec:
      component: dev
      commandLine: "echo 'Static web content - no build required'"
  - id: test
    exec:
      component: dev
      commandLine: "echo 'No tests configured'"